// Common cross-cutting types shared across all domains
module {
  public type Timestamp = Int; // nanoseconds since epoch (Time.now())
  public type ResumeId = Text;
  public type MatchId = Text;
};
