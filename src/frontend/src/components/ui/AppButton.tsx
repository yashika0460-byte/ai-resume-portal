/**
 * AppButton — re-exports shadcn Button with additional app-specific variants.
 *
 * All page code should import from here, not directly from @/components/ui/button,
 * so that variant additions are centralized.
 */

export { Button, buttonVariants } from "@/components/ui/button";
